1. SQLAlchemy provides a python abstraction that makes it easy to manipulate database. Programmers no longer need to care about different database versions, types, SQL dialect, etc. They just ues the same command regardless of whether they are interacting with PostgreSQL, SQLite, MySQL, or some other relational database.
2. The model here is the shcema of the database.
3. A database view is a subset of a database and is based on a query that runs on one or more database tables. Database views are saved in the database as named queries and can be used to save frequently used, complex queries.
